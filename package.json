{
  "name": "ur-home-taste",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready full-stack ecommerce platform for Ur Home Taste.",
  "scripts": {
    "dev": "npm run dev --workspace @ur-home-taste/api & npm run dev --workspace @ur-home-taste/web",
    "build": "npm run build --workspace @ur-home-taste/database && npm run build --workspace @ur-home-taste/api && npm run build --workspace @ur-home-taste/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace @ur-home-taste/database",
    "db:migrate": "npm run db:migrate --workspace @ur-home-taste/database",
    "db:seed": "npm run db:seed --workspace @ur-home-taste/database"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "postcss": "^8.5.15",
    "next": {
      "postcss": "^8.5.15"
    }
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
